import knex from '../database';
/**
 * Load user with `username` and `password`
 * @param {object} options
 * @param {string} options.username - username of the user
 * @param {string} options.password - plain text password
 * @return 
 */
export async loadUser({username, password}) {
  
}
